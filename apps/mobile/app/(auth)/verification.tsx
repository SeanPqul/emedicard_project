// Verification route - Thin route file following clean architecture
import { VerificationScreen } from '@features/auth';

export default function VerificationRoute() {
  return <VerificationScreen />;
}
