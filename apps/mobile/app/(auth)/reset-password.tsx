// Reset password route - Thin route file following clean architecture
import { ResetPasswordScreen } from '@features/auth';

export default function ResetPasswordRoute() {
  return <ResetPasswordScreen />;
}
