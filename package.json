{
  "name": "emedicard_project",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "pnpm": {
    "allowedScriptVersions": {
      "@clerk/shared": "*",
      "@tailwindcss/oxide": "*",
      "browser-tabs-lock": "*",
      "core-js": "*",
      "esbuild": "*",
      "sharp": "*",
      "unrs-resolver": "*"
    }
  }
}
