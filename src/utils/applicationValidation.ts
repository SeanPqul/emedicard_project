// Re-export from shared utilities for backward compatibility
export {
  type ApplicationType,
  type CivilStatus,
  type ApplicationFormData as FormData,
  validateApplicationStep,
  validateFormData,
} from '../shared/validation/form-validation';